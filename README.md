# ejemplo10

## Project setup
```
src/store.js -> defines a simple store with 1 value to be stored through the application : myVar
src/views/Home.vue -> displays the value from the store in the screen
src/views/About.vue -> allows the user to edit the store's value
